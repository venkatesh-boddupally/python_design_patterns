-> strategy pattern is a behavioral pattern.
-> Enable client to use different set of algorithms to complete task.

--------------------------------------

-> enable algorithm behavior at run time.
-> define set of algorithms, encapsulates each algorithm and makes algorithm interchangeable